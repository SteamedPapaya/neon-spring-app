spring:
  application:
    name: chat
  profiles:
    active: dev

server:
  port: 8080

management:
  endpoint:
    prometheus:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health,info,prometheus